interface LemonWM {
    CreateWindow(string title, s32 x, s32 y, s32 width, s32 height, uint32_t flags)
    DestroyWindow()

    SetTitle(string title)
    Relocate(vector2i_t pos)
    Resize(vector2i_t size) -> (u32 bufferKey)
    Minimize(int windowID, bool minimized)
}
